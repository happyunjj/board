<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board.repository.BoardMapper">
    <select id="getList" resultType="Board">
        SELECT BNO,TITLE,CONTENT,TO_CHAR(REGDATE,'yyyy-MM-dd hh24:mi:ss') REGDATE,BOOKMARK
        FROM TBL_BOARD
        <if test="bnos != null">
            WHERE BNO IN
            <foreach collection="array" item="bookmarks"  open="(" close=")" separator=",">
                #{bookmarks}
            </foreach>
        </if>
        ORDER BY BNO DESC
    </select>

    <select id="readBoardPost" resultType="Board">
        SELECT BNO,TITLE,CONTENT,TO_CHAR(REGDATE,'yyyy-MM-dd hh24:mi:ss') REGDATE,TO_CHAR(MODDATE,'yyyy-MM-dd hh24:mi:ss') MODDATE FROM TBL_BOARD
        WHERE BNO = #{bno}
    </select>
    
    <insert id="write">
        INSERT INTO TBL_BOARD (TITLE,CONTENT) VALUES (#{title},#{content})
    </insert>
    
    <update id="update">
        UPDATE TBL_BOARD
        SET TITLE = #{title},
            CONTENT = #{content},
            MODDATE = CURRENT_TIMESTAMP
        WHERE BNO = #{bno}
    </update>
    <delete id="delete">
        DELETE TBL_BOARD
        WHERE BNO = #{bno}
    </delete>
</mapper>